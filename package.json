{
  "name": "snowball",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "open-props": "^1.4.16",
    "turbo": "^1.5.6"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}